version: '3.8'

services:
  notifications-service:
    container_name: connectfy-notifications-service-prod
    image: connectfy/connectfy-notifications-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    depends_on:
      - mongo
    restart: always
    networks:
      - connectfy-notifications-network
      - notifications-db-network
      - connectfy-api-gateway-network
      - connectfy-kafka-network
    environment:
      - NODE_ENV=production
    env_file:
      - .env.production

  mongo:
    image: mongo:6.0
    container_name: connectfy-notifications-mongo-prod
    volumes:
      - mongo-data:/data/db
    networks:
      - connectfy-notifications-network
      - notifications-db-network
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: notificationsDb
    ports:
      - '25201:27017'

volumes:
  mongo-data:

networks:
  connectfy-notifications-network:
    external: true
  notifications-db-network:
  connectfy-api-gateway-network:
    external: true
  connectfy-kafka-network:
    external: true
